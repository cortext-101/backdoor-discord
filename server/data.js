export const servers = {}; 
// structure: { serverId: { name, channels: { channelId: { name, messages: [] } }, users: [] } }

export const users = {}; 
// { socketId: { username, currentServer, currentChannel } }
